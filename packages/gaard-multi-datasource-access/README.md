# Gaard Multi Datasource Access

Private GAARD enterprise extension for query behavior spanning multiple active
datasources.

It registers the `query` contribution and provides:

- multi-active datasource selection,
- datasource-qualified schema prompt formatting,
- multi-dialect planning,
- SQL routing and transpilation to the target connector dialect.
